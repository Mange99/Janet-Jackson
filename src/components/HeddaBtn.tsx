import { Box, Button } from "@chakra-ui/react";

const HeddaBtn = () => {
    return (
      <Box>
        <Button _focus={{ boxShadow: 0 }}>Hedda</Button>
      </Box>
    );
  };
  
  export default HeddaBtn;